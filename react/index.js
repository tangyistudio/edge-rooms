/**
 * edge-rooms/react — React entry point.
 *
 * `react` is an optional peer dependency; this entry point is the only part
 * of the package that needs it.
 */

export { useRoom } from './useRoom.js';
